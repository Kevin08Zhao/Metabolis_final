task_id: T-08
owner: ACCOUNT_C
status: DONE
base_main_commit: aa66efb50087b0b0988f91337ba26fe79ac9e3f9
source: docs/prompts/Metabolis_Prompts_Full_v2.md · T-08 · EVENT_API.md event and signal list
upstream:
  - docs/CONTEXT.md (naming conventions section, pasted)
  - docs/GAME_RULES.md (full text, pasted)
outputs:
  - docs/EVENT_API.md
checks:
  - All six required table columns present (event name, trigger moment, parameters and types, frequency, suggested animation, suggested audio): PASS
  - All seven required moment families covered, laid out as sections 1 through 7 — stage advance and snapshot write, 4 events; build options presented / selected / construction started / construction complete, 4 events; operation decision submission and resource priority change, 5 events; the three bottleneck types appearing and clearing, 6 events; stability band change / waste overflow / investable resource shortage, 4 events; minigame entry, exit and star rating, 3 events; knowledge unlock / system observation start and end / carryover application, 5 events. Plus 1 action rejection event, 32 total: PASS
  - Every event name is snake_case, follows {subject}_{past_tense}, and is at most three words long: PASS
  - The frequency column is restricted to four values and marks every event that can fire more than once in the same tick (organ_built, resource_priority_changed, the six bottleneck events, resource_shortage_raised/_cleared, knowledge_entry_unlocked/_opened, action_rejected): PASS
  - The four retired event families (manual route connection, supply testing, maintenance choice, maintenance delayed effect) are not defined, and a dedicated "Retired events that must not be reintroduced" section states the replacement mount point for each: PASS
  - No event is defined outside the docs/GAME_RULES.md rules table; the closing cross-check table maps the visible feedback and rejection feedback of all six actions to events one by one: PASS
  - Original prompt acceptance item "paste the signal block into an empty .gd file and confirm there is no syntax error": PASS. The thirty-two signal declarations were pasted into src/autoload/event_bus.gd and parsed by Godot 4.7.1.stable with 0 errors and 0 warnings in the Output panel
  - Live run: Godot 4.7.1.stable imported /Users/michaelmas/Desktop/Metabolis/src and ran the smoke scene; four signals covering 1, 2, 3 and 4 parameters all emitted correctly: PASS
revalidations:
  - revalidated_at: 2026-07-27T16:40:00-04:00
    base_main_commit: 6958bbe
    trigger: docs/CARRYOVER_SPEC.md (T-05f) landed on main after this task passed. Its tables F1 and F2 were placeholders when T-08 was written, so the two rows that reference them were rechecked against the delivered document.
    findings:
      - carryover_applied correctly points at table F1, whose three values are network_efficiency_coefficient, initial_operation_pressure and initial_waste_accumulation. No change needed beyond naming them explicitly.
      - stage_snapshot_written was wrong on timing. It claimed the event fires before the transition of stage_advanced completes, taking the "generates the carryover snapshot" immediate effect of advance_to_next_stage in docs/GAME_RULES.md as the moment of persistence. Table F2 commits the record when the next stage is entered for the first time, which is after stage_loaded. Generation and persistence are two distinct moments.
      - stage_snapshot_written carried a snapshot_slot parameter that no upstream defines. Table F2 keys snapshots by stage id, not by slot.
      - Neither event fires on replay for the snapshot write, because the T-19h replay rule of docs/CARRYOVER_SPEC.md reads chapter_snapshots directly and recalculates nothing.
    changes:
      - Reordered section 1 so document order matches runtime order: stage_advanced, stage_loaded, stage_snapshot_written, phase_changed.
      - Rewrote the stage_snapshot_written trigger to the commit of the table F2 atomic save transaction, and its parameters to (snapshot_stage_id: StringName, snapshot: Dictionary). The snapshot_slot parameter was removed.
      - Rewrote the carryover_applied trigger to the F1 per-value application positions, ending before the first E3 settlement tick.
      - Added two explanatory paragraphs to section 1: "Generation versus persistence" and "Replay".
      - Updated the two affected rows of the rules-table cross-check.
    checks:
      - Event count unchanged at 32; no event added or removed: PASS
      - Live headless run on Godot 4.7.1.stable after the change, exercising the new two-parameter stage_snapshot_written signature: PASS
      - verify_signal_set() returns { "missing": [], "extra": [] }: PASS
    no_rework_filed: The defect was in this account's own file, not in T-05f. docs/CARRYOVER_SPEC.md is correct as delivered, so no OPEN rework file was created against Account B.
  - revalidated_at: 2026-07-27T19:45:00-04:00
    base_main_commit: 83e2b45
    trigger: T-20 delivered the birth state machine and found that docs/EVENT_API.md defines no birth event, leaving the climax of the game without an animation or audio mount point. All seven T-21 tasks state that they emit events and play nothing, so there was nothing for five of them to emit.
    finding: |
      T-08 was constrained to events present in the rules table of
      docs/GAME_RULES.md, which covers the six player actions. The birth sequence
      contains no player action, so no birth event was defined. That reading was
      correct at the time but too narrow once the birth documents existed: the
      constraint exists to stop events being invented for moments with no source,
      and these moments now have three accepted sources - final_completion_ready
      in docs/CHAPTER_TIMELINE.md, the four birth checks of table E5 in
      docs/OPERATION_SPEC.md, and the seven states and timeline of
      docs/BIRTH_STATES.md.
    changes:
      - New section 9, Birth transition, with four events - birth_sequence_started, birth_state_changed, birth_sequence_completed, birth_rolled_back - and their sources named at the top of the section.
      - birth_state_changed is the generic per-beat mount point, structurally the same as phase_changed. It carries window_ms so D-22 and D-26 can time a beat without reading Balance.
      - Illegal transitions get no event of their own; they reuse action_rejected with action_id birth_transition.
      - The signal declaration block grows from thirty-two lines to thirty-six.
      - A note under the rules-table cross-check records why the birth sequence has no row there.
    checks:
      - Event count 32 to 36; no existing event changed, renamed, or removed: PASS
      - Every new name is snake_case, past tense, at most three words: PASS
      - Live headless run on Godot 4.7.1.stable exercising all four new events across the happy path, the gate failure path, and a mid-sequence precondition loss: PASS
      - verify_signal_set() returns { "missing": [], "extra": [] } with EVENT_NAMES at 36: PASS
    scope: Additive. No downstream consumer of the original thirty-two events is affected.
  - revalidated_at: 2026-07-28T02:15:00-04:00
    base_main_commit: fa128a7
    trigger: A full Account C audit found that birth_sequence_started fired only on the first attempt. See the revalidations block of docs/coord/done/T-22.md for the full report.
    finding: |
      This document's row for birth_sequence_started read "the machine enters
      ready_check", which is correct. The implementation in birth_machine keyed on
      leaving IDLE instead, so a retry after a rollback never announced itself and
      the input lock stayed open through the whole retried finale.
      The contract was right and the code was wrong, but the frequency column was
      also wrong: it read "once per run" when a retry legitimately starts a new
      attempt.
    changes:
      - The birth_sequence_started row now states that it fires on every entry into ready_check, from start() or from an acknowledged rollback, and the frequency reads once per attempt rather than once per run, with the reason given.
    checks:
      - Signal count unchanged at 36; no event added, renamed or removed: PASS
      - birth_sequence_started observed firing twice across one failed and one successful attempt: PASS
  - revalidated_at: 2026-07-28T03:00:00-04:00
    base_main_commit: 3a0a0ab
    trigger: T-24 needed a hint that carries the decision identifiers and the stage that produced a carryover value, and required event names to match this document.
    finding: |
      No existing event could carry both. knowledge_entry_unlocked takes an entry,
      an organ and a stage, so using it would have meant lying about what those
      parameters mean. The immediate knowledge hints of table E11 share a UI
      container with delayed feedback, but sharing a container is not the same as
      sharing an event.
    changes:
      - New event delayed_feedback_shown(carryover_field, source_stage_id, source_decision_ids) in section 7, next to carryover_applied.
      - Frequency stated as at most once per carryover field per stage, so at most three per stage and never in stage one, which has no predecessor.
      - The row records that it renders in the same non-blocking container as the table E11 immediate hints.
      - Signal count 36 to 37.
    checks:
      - Name is snake_case, past tense, three words: PASS
      - No existing event changed, renamed or removed: PASS
      - verify_signal_set() clean with EVENT_NAMES at 37: PASS
      - Live headless run exercising the event across five scenarios: PASS
    scope: Additive. No consumer of the previous 36 events is affected.
  - revalidated_at: 2026-07-28T04:30:00-04:00
    base_main_commit: 00572d8
    trigger: T-25 needed an event marking the whole run being over with its summary built, and required event names to match this document.
    finding: |
      birth_sequence_completed marks only the ending picture starting. The run
      being over is a different moment: five other completion conditions and the
      full ending window later. Reusing it would have collapsed two states the
      game deliberately keeps apart, and would have handed listeners a summary
      that did not exist yet.
    changes:
      - New event season_completed(summary: Dictionary) in section 7. The row states that the payload carries no score, rating, or title, matching the T-25 prohibition.
      - Signal count 37 to 38.
    checks:
      - Name is snake_case, past tense, two words: PASS
      - No existing event changed, renamed or removed: PASS
      - verify_signal_set() clean with EVENT_NAMES at 38: PASS
      - Live headless run: emitted exactly once on closure and not re-emitted when closure was attempted again: PASS
    scope: Additive. No consumer of the previous 37 events is affected.
interface_changes: |
  Revision of 2026-07-27 changes one signal signature:
    stage_snapshot_written(stage_id: StringName, snapshot_slot: int, snapshot: Dictionary)
    becomes
    stage_snapshot_written(snapshot_stage_id: StringName, snapshot: Dictionary)
  No consumer existed yet, so nothing downstream had to be migrated. T-19d, T-19h,
  T-26, T-27, and T-28 must read the revised section 1 rather than the original.
  Otherwise NONE: docs/EVENT_API.md remains the single upstream for D-18, T-37, and
  every animation and audio mount point.
notes: |
  The marker path follows the MARKER PROTOCOL on page 5 of the Account C task
  guide PDF (version 2.0), i.e. docs/coord/done/T-08.md. Handshake-era markers in
  docs/coord/ keep the older <task>.done form; this account uses the PDF form only
  and did not modify docs/coord/README.md or any existing *.done file owned by
  Account A.
completed_at: 2026-07-27T15:12:47-04:00
