task_id: T-13a
owner: ACCOUNT_B
status: DONE
base_main_commit: af2cc28bd551d135c18db87ee0edf0943344beeb
outputs:
- src/ui/option_preview.gd
checks:
- Listens to GridManager slot_hovered and slot_unhovered signals without modifying the grid script: PASS
- Resolves every hovered slot through an explicit decision_id and option_id context: PASS
- Reads all raw metrics, units, and ranges through the canonical Balance paths: PASS
- Displays network efficiency, build duration, and future convenience in the required fixed order: PASS
- Displays concrete numbers and units without recommendations, ranking, or qualitative judgments: PASS
- Uses the exact Table D8 normalization formula, including the reversed build-duration bar: PASS
- Uses each configured range maximum as its ProgressBar maximum: PASS
- Keeps an out-of-range raw number visible while clamping only its comparison bar: PASS
- Builds three default-system rows with Labels and ProgressBars and no art dependency: PASS
- Focused hover test verifies three candidates, row order, exact numbers, exact ratios, clamping, and hide behavior: PASS
- Scene setup and reproducible acceptance steps are embedded in the English-only production file: PASS
interface_changes: |
  Adds class OptionPreview at src/ui/option_preview.gd.
  Public interface:
    configure(balance_access: Node, grid_manager: Node) -> void
    set_candidates(candidate_contexts: Dictionary) -> void
    displayed_values() -> Vector3
    bar_ratios() -> Vector3
    show_candidate(candidate_id: StringName) -> bool
completed_at: 2026-07-28T08:31:00+08:00
