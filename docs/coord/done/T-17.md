task_id: T-17
owner: ACCOUNT_B
status: DONE
base_main_commit: 9a1771fc27dc8d9a9c744c941cda08ca6658947a
outputs:
  - src/sim/resource_tick.gd
checks:
  - Runs deterministic fixed-interval settlement without _process: PASS
  - Preserves the required production, transport, consumption, waste, and stability order: PASS
  - Reads tick interval, resource rates, ranges, weights, and denominators through Balance: PASS
  - Settles requested resource and development-signal flow against active graph connectivity, available tick flow, and shared residual edge capacity: PASS
  - Targets the highest-sequence T-15a terminal node and cannot bypass a zero-capacity interior edge through a co-located entry node: PASS
  - Uses NetworkCoverage for exact per-organ E3 delivered-to-required coverage: PASS
  - Calculates weighted city transport coverage, signal coverage, transport pressure, waste, and stability directly from E3: PASS
  - Uses explicit per-organ active, tier, and resource-satisfaction multipliers for E3 waste generation and processing: PASS
  - Keeps all six resources in range across thirty consecutive ticks and leaves knowledge badges unchanged: PASS
  - Changing a Balance production rate changes the resulting resource curve: PASS
  - Exposes per-organ coverage, settled flows, operational metrics, and the three-field T-19h carryover source snapshot as read-only copies: PASS
  - Emits no threshold events and contains no threshold watcher behavior: PASS
  - Godot 4.7.1 editor parse and headless project smoke checks: PASS
  - English-only, secret-pattern, and git diff checks: PASS
interface_changes: |
  Adds class ResourceTick at src/sim/resource_tick.gd.
  Public interface:
    configure(balance_access: Node, coverage_calculator: RefCounted) -> void
    initialize_from_balance(overrides: Dictionary = {}) -> void
    advance_time(elapsed_seconds: float, settlement_input: Dictionary) -> int
    settle_tick(tick_delta: float, settlement_input: Dictionary) -> Dictionary
    run_acceptance_test() -> Array[Dictionary]
  Read-only properties:
    resources
    organ_transport_coverage
    settled_delivered_flow
    settled_delivered_development_signal
    settled_edge_flow
    transport_coverage
    transport_pressure
    signal_coverage
    carryover_source_snapshot
completed_at: 2026-07-28T09:37:13+08:00
