task_id: T-10
owner: ACCOUNT_C
status: DONE
base_main_commit: aa66efb50087b0b0988f91337ba26fe79ac9e3f9
source: docs/prompts/Metabolis_Prompts_Full_v2.md · T-10 · EventBus 事件总线单例
upstream:
  - docs/coord/done/T-08.md
  - docs/EVENT_API.md（signal 声明列表，已粘贴）
outputs:
  - src/autoload/event_bus.gd
  - src/autoload/event_bus.gd.uid（Godot 4.7 为该脚本生成的 UID 伴随文件，与脚本同属一个资产）
checks:
  - 只产出一个脚本文件，内容仅有信号声明与调试辅助，无任何游戏逻辑：PASS
  - 信号集合与 docs/EVENT_API.md 完全一致，共 32 个，未新增清单外信号：PASS。脚本自带 verify_signal_set() 自检，实际运行返回 { "missing": [], "extra": [] }
  - 调试模式开启时每次信号发出都打印统一前缀 [EVENT] 加事件名与参数：PASS。实测输出四条，覆盖 1/2/3/4 四种参数个数的分发路径：
      [EVENT] transport_pressure_cleared [&"edge_01"]
      [EVENT] stage_advanced [&"stage_01_origin", &"stage_02_harbor"]
      [EVENT] organ_built [&"organ_heart", &"slot_a", &"option_a"]
      [EVENT] build_decision_confirmed [&"bd_01", &"option_a", &"slot_a", { &"nutrient_energy": 3.0 }]
  - 调试模式关闭时不产生任何输出：PASS。debug_enabled 置为 false 后再次发出 stage_advanced，控制台无 [EVENT] 行
  - 调试开关为可在运行时修改的普通变量 debug_enabled，未使用条件编译：PASS
  - 未使用 Godot 3 API，未引入任何插件：PASS
  - 实际运行：Godot 4.7.1.stable 打开 /Users/michaelmas/Desktop/Metabolis/src，注册 EventBus 为 autoload 后运行冒烟场景，Output 面板 0 error / 0 warning：PASS
interface_changes: |
  新增 autoload 单例 EventBus，下游一律通过 EventBus.<事件名>.emit(...) 与
  EventBus.<事件名>.connect(...) 收发事件，不再互相引用节点。
  调试开关为 EventBus.debug_enabled（bool，默认 false，可运行时修改）。
  自检接口为 EventBus.verify_signal_set() -> Dictionary，返回 missing 与 extra 两个键。
  注册步骤（本提交不含 src/project.godot 的改动，见 notes）：
    Godot 编辑器 → 项目 → 项目设置 → 全局（Globals / Autoload）标签
    → 路径填 res://autoload/event_bus.gd，节点名填 EventBus，勾选启用 → 添加。
  等价的 src/project.godot 片段：
    [autoload]

    EventBus="*res://autoload/event_bus.gd"
notes: |
  按 v2.2 单文件限制的申报规则，本条需要改动的其他文件只有 src/project.godot（新增上面三行）。
  该改动已在本地完成并用于实机验收，但按项目负责人指示不纳入本提交，仅保留在本地工作区。
  其他账号在拉取本提交后必须自行完成上述 autoload 注册，否则 EventBus 标识符不存在、下游脚本无法解析。
  marker 路径按账号 C 任务指引 PDF（版本 2.0）第 5 页的 MARKER PROTOCOL 采用 docs/coord/done/T-10.md。
completed_at: 2026-07-27T15:12:47-04:00
