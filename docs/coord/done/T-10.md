task_id: T-10
owner: ACCOUNT_C
status: DONE
base_main_commit: aa66efb50087b0b0988f91337ba26fe79ac9e3f9
source: docs/prompts/Metabolis_Prompts_Full_v2.md · T-10 · EventBus event bus singleton
upstream:
  - docs/coord/done/T-08.md
  - docs/EVENT_API.md (signal declaration block, pasted)
outputs:
  - src/autoload/event_bus.gd
  - src/autoload/event_bus.gd.uid (UID companion file generated by Godot 4.7 for that script; part of the same asset)
checks:
  - Exactly one script file produced, containing signal declarations and debug helpers only, with no game logic: PASS
  - The signal set matches docs/EVENT_API.md exactly, 32 signals, with nothing added beyond the document: PASS. The script ships a verify_signal_set() self-check, which returned { "missing": [], "extra": [] } on a live run
  - With the debug switch on, every emit prints the shared prefix [EVENT] followed by the event name and parameters: PASS. Four lines observed, covering all four dispatch paths (1, 2, 3 and 4 parameters):
      [EVENT] transport_pressure_cleared [&"edge_01"]
      [EVENT] stage_advanced [&"stage_01_origin", &"stage_02_harbor"]
      [EVENT] organ_built [&"organ_heart", &"slot_a", &"option_a"]
      [EVENT] build_decision_confirmed [&"bd_01", &"option_a", &"slot_a", { &"nutrient_energy": 3.0 }]
  - With the debug switch off, no output is produced: PASS. After setting debug_enabled to false, stage_advanced was emitted again and no [EVENT] line appeared
  - The debug switch is a plain variable, debug_enabled, changeable at runtime; no conditional compilation: PASS
  - No Godot 3 API is used and no plugin is introduced: PASS
  - Live run: Godot 4.7.1.stable opened /Users/michaelmas/Desktop/Metabolis/src, EventBus was registered as an autoload, and the smoke scene ran with 0 errors and 0 warnings in the Output panel: PASS
revalidations:
  - revalidated_at: 2026-07-27T16:40:00-04:00
    base_main_commit: 6958bbe
    trigger: docs/EVENT_API.md was revised after docs/CARRYOVER_SPEC.md (T-05f) landed. See the revalidations block of docs/coord/done/T-08.md for the finding.
    changes:
      - stage_snapshot_written signature changed from (stage_id: StringName, snapshot_slot: int, snapshot: Dictionary) to (snapshot_stage_id: StringName, snapshot: Dictionary). The dispatch path therefore moves from _log_3 to _log_2; both dispatchers already existed and neither was modified.
      - EVENT_NAMES reordered so it still matches document order after section 1 was reordered.
    checks:
      - Signal count unchanged at 32: PASS
      - Live headless run on Godot 4.7.1.stable, emitting the new two-parameter stage_snapshot_written and observing its [EVENT] line: PASS
      - verify_signal_set() returns { "missing": [], "extra": [] }, confirming the script and docs/EVENT_API.md are still in exact agreement: PASS
      - Debug switch off still produces no output: PASS
  - revalidated_at: 2026-07-27T19:45:00-04:00
    base_main_commit: 83e2b45
    trigger: docs/EVENT_API.md gained section 9, Birth transition. See the second revalidation entry of docs/coord/done/T-08.md.
    changes:
      - Four signals added - birth_sequence_started, birth_state_changed, birth_sequence_completed, birth_rolled_back - with matching EVENT_NAMES entries. Signal count 32 to 36.
      - No dispatcher changed. The new arities are 2 and 3, both already covered by _log_2 and _log_3.
    checks:
      - verify_signal_set() returns { "missing": [], "extra": [] } with EVENT_NAMES at 36, confirming the script and docs/EVENT_API.md still agree exactly: PASS
      - Live headless run on Godot 4.7.1.stable, 0 script errors: PASS
  - revalidated_at: 2026-07-28T03:00:00-04:00
    base_main_commit: 3a0a0ab
    trigger: docs/EVENT_API.md gained delayed_feedback_shown for T-24. See the matching revalidation entry in docs/coord/done/T-08.md.
    changes:
      - One signal added with its EVENT_NAMES entry. Signal count 36 to 37.
      - No dispatcher changed; the new arity is 3, already covered by _log_3.
    checks:
      - verify_signal_set() returns empty missing and extra with EVENT_NAMES at 37: PASS
      - Live headless run on Godot 4.7.1.stable, 0 script errors: PASS
  - revalidated_at: 2026-07-28T04:30:00-04:00
    base_main_commit: 00572d8
    trigger: docs/EVENT_API.md gained season_completed for T-25. See the matching revalidation entry in docs/coord/done/T-08.md.
    changes:
      - One signal added with its EVENT_NAMES entry. Signal count 37 to 38.
      - No dispatcher changed; the new arity is 1, already covered by _log_1.
    checks:
      - verify_signal_set() returns empty missing and extra with EVENT_NAMES at 38: PASS
      - Live headless run on Godot 4.7.1.stable, 0 script errors: PASS
  - revalidated_at: 2026-07-28T06:30:00-04:00
    base_main_commit: 3bc2419
    trigger: docs/EVENT_API.md gained save_loaded for T-27. See the matching revalidation entry in docs/coord/done/T-08.md.
    changes:
      - One signal added with its EVENT_NAMES entry. Signal count 38 to 39.
      - No dispatcher changed; the new arity is 2, already covered by _log_2.
    checks:
      - verify_signal_set() returns empty missing and extra with EVENT_NAMES at 39: PASS
      - Live headless run on Godot 4.7.1.stable, 0 script errors: PASS
interface_changes: |
  Revision of 2026-07-27: stage_snapshot_written now takes
  (snapshot_stage_id: StringName, snapshot: Dictionary). No consumer existed yet.

  Adds the autoload singleton EventBus. Downstream code sends and receives events
  through EventBus.<event_name>.emit(...) and EventBus.<event_name>.connect(...)
  instead of referencing nodes directly.
  Debug switch: EventBus.debug_enabled (bool, defaults to false, changeable at runtime).
  Self-check interface: EventBus.verify_signal_set() -> Dictionary, returning the
  keys "missing" and "extra".
  Registration steps (this commit does not include the src/project.godot change; see notes):
    Godot editor -> Project -> Project Settings -> Globals (Autoload) tab
    -> path res://autoload/event_bus.gd, node name EventBus, enable it -> Add.
  Equivalent src/project.godot fragment:
    [autoload]

    EventBus="*res://autoload/event_bus.gd"
notes: |
  Under the declaration rule that replaced the v2.2 single-file restriction, the
  only other file this task needs to touch is src/project.godot (the three lines
  above). That change was made locally and used for live acceptance, but by the
  project owner's instruction it is not part of this commit and remains in the
  local working tree only.
  Other accounts must perform the autoload registration themselves after pulling
  this commit; otherwise the EventBus identifier does not exist and downstream
  scripts fail to parse.
  The marker path follows the MARKER PROTOCOL on page 5 of the Account C task
  guide PDF (version 2.0), i.e. docs/coord/done/T-10.md.
completed_at: 2026-07-27T15:12:47-04:00
