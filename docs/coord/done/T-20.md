task_id: T-20
owner: ACCOUNT_C
status: DONE
base_main_commit: c8b523a
source: docs/prompts/Metabolis_Prompts_Full_v2.md · T-20 · birth transition state machine skeleton
upstream:
  - docs/coord/done/T-19e.md (status DONE, delivered src/sim/birth_check.gd)
  - docs/coord/done/T-19.md (status DONE, delivered src/world/organ_check.gd)
  - docs/coord/done/T-08.md (status DONE)
  - docs/CHAPTER_TIMELINE.md (stage four configuration, pasted)
  - docs/OPERATION_SPEC.md (table E5 four birth thresholds, pasted)
  - docs/EVENT_API.md (birth-related event rows, pasted - see the known gap below)
outputs:
  - docs/BIRTH_STATES.md
  - src/sim/birth_machine.gd
  - src/sim/birth_machine.gd.uid
checks:
  - Seven states present - ready_check, umbilical_stop, pulmonary_flow, fetal_shunts, systems_online, ending, failure_rollback - plus IDLE as the pre-start state: PASS
  - State definition document carries all seven required columns - state name, entry condition, performed on entry, exit condition, forbidden player actions, event emitted, start and end milliseconds on the 45-second timeline: PASS (table B1)
  - Three observable phases at exactly 10000 ms each, ending picture 15000 ms, total exactly 45000 ms rather than merely under it: PASS (table B2). ready_check and failure_rollback carry no window; the gate resolves before the sequence and the rollback is an interrupt, so placing either on the timeline would push the total past budget
  - One production file. Every state change routes through transition_to and _current_state is assigned in exactly one place: PASS
  - Every transition prints [BIRTH] old state, new state and emits its event: PASS
  - All seven state bodies are exactly pass. Verified mechanically, not by eye: PASS
  - No hardcoded gameplay parameter. All five window lengths are read through Balance at chapters.stage_birth.birth_sequence.*: PASS
  - Original prompt acceptance item, transition list with legal paths as arrows and illegal ones marked: PASS (tables B3 and B4)
  - Original prompt acceptance item, 45-second allocation table with start and end milliseconds: PASS (table B2)
  - Original prompt acceptance item, illegal transitions rejected on a live run - eight rejections observed, each leaving the state untouched - skipping into the sequence from IDLE, jumping to the ending, re-entering the current state, skipping a beat forward, rewinding, revoking success from the ending, restarting from the ending, and resuming mid-sequence after a rollback: PASS
  - Happy path runs end to end - idle, ready_check, umbilical_stop, pulmonary_flow, fetal_shunts, systems_online, ending - and ending reports terminal: PASS
  - Failure path never ends the run. A failed gate routes to failure_rollback, which is not terminal and returns to ready_check on acknowledgement, honouring the guarantee in docs/OPERATION_SPEC.md that a failed check does not lock the flow: PASS
  - failure_rollback reachable from every non-terminal beat and from nowhere else: PASS
  - Live run on Godot 4.7.1.stable, headless, 0 script errors: PASS
interface_changes: |
  Adds class BirthMachine (Node) at src/sim/birth_machine.gd.
    start() -> bool
    transition_to(next_state: int) -> bool      # sole mutator
    can_transition_to(next_state: int) -> bool
    submit_gate_result(all_checks_passed: bool) -> bool
    acknowledge_rollback() -> bool
    current_state() -> int
    current_state_id() -> StringName
    is_terminal() -> bool
    gate_passed() -> bool
    state_duration_ms(state: int) -> int
    total_timeline_ms() -> int
    missing_duration_paths() -> PackedStringArray
    signal birth_state_changed(previous_state: int, current_state: int)
    enum State with eight values; STATE_IDS gives the persisted spellings;
    LEGAL_TRANSITIONS is the graph of table B3
  T-21-1 through T-21-7 each fill exactly one _on_enter_* body and must not touch
  the graph, the routing, or another state. T-22 reads current_state to drive the
  input lock.
blocking_dependency: |
  docs/BALANCE.json has no birth sequence timings. Six keys are required, owned by
  T-06 and therefore by ACCOUNT_B:
    chapters.stage_birth.birth_sequence.umbilical_stop_ms   = 10000
    chapters.stage_birth.birth_sequence.pulmonary_flow_ms   = 10000
    chapters.stage_birth.birth_sequence.fetal_shunts_ms     = 10000
    chapters.stage_birth.birth_sequence.systems_online_ms   = 5000
    chapters.stage_birth.birth_sequence.ending_ms           = 10000
    chapters.stage_birth.birth_sequence.total_budget_ms     = 45000
  The values are the ones table B2 of docs/BIRTH_STATES.md fixes. Until they exist
  the machine reports them through missing_duration_paths(), state_duration_ms()
  returns zero, and total_timeline_ms() returns zero.
  T-20 does not need them: the graph, the routing, and the rejection rules are all
  independent of duration, which is why this task passed acceptance without them.
  T-21-2 onward do need them, because that is where a window actually elapses.
  This is recorded rather than worked around; no key was invented locally and no
  default was baked into the script.
known_gap: |
  docs/EVENT_API.md defines no birth event. T-08 was constrained to events present
  in the rules table of docs/GAME_RULES.md, which covers the six player actions,
  and the birth sequence contains no player action.
  This machine therefore emits its own class signal birth_state_changed rather
  than routing state changes through EventBus. Rejections do use EventBus through
  action_rejected, which fits without stretching its meaning; eight were observed
  on the acceptance run carrying action_id birth_transition, a reason code, and
  the unchanged current state as the focus element.
  That is workable for T-20 through T-22, but it leaves the climax of the game
  without an animation and audio mount point, which is the entire purpose of
  docs/EVENT_API.md. D-24 and the ending animation work will need one. Adding
  birth rows to docs/EVENT_API.md is a T-08 revalidation and is left as a declared
  follow-up rather than done silently inside T-20.
notes: |
  ready_check is deliberately off the 45-second timeline. The prompt budgets three
  observable phases at about ten seconds each plus an ending picture of about
  fifteen, which is 45000 ms with nothing left over. The gate animates nothing and
  resolves before the sequence begins, so giving it a window would have forced
  either the observable phases or the ending under their stated lengths.
  No file owned by another account was modified. src/project.godot is unchanged.
  docs/coord/rework/ contains no OPEN file against any upstream of this task.
completed_at: 2026-07-27T19:10:00-04:00
