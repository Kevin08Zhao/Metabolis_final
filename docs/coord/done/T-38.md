task_id: T-38
task_name: Debug switches and no-art validation
owner: ACCOUNT_C
status: DONE
base_main_commit: cffeb595ee926a6e5a162c69acb6b0416042a7f0
source: docs/prompts/Metabolis_Prompts_Full_v2.md - T-38
upstream:
  - docs/coord/done/D-23.md
  - docs/FALLBACK_SPEC.md
  - docs/coord/done/T-36.md
  - docs/coord/done/T-37.md
outputs:
  - src/core/debug_flags.gd
  - docs/NO_ART_TEST.md
declared_changes_outside_the_original_output: |
  The user explicitly authorized src/project.godot after the file and reason
  were declared. The entry registers DebugFlags as an autoload so its hotkeys,
  scene-wide replacement, and newly-created-node handling exist in the shipped
  runtime. AssetLoader and AudioRouter were not modified.
checks:
  - The focused verifier detected its deliberate 1 == 2 failure and passed its comment-stripper fixture before any verdict was trusted: PASS
  - All three switches default false, toggle independently, combine freely, and restore their prior presentation state without a scene restart: PASS
  - F8 freezes the strained-heart fixture on D-23 frame 2 rather than frame 0, stops particle emission, and leaves a gameplay Timer and audio state running: PASS
  - F9 uses AudioRouter's existing mute interface; silence does not stop gameplay signals, timers, or the birth machine: PASS
  - F10 replaces current and subsequently created formal textures with same-size, high-contrast placeholders and adds identifiers where space permits; restoration returns the exact original resources: PASS
  - Build completion gates Continue and Space for a measured 8.0 seconds; system collaboration gates them for a measured 12.0 seconds; both restore the prior button state: PASS
  - Birth animation-disabled behavior displays the five D-23 state fallbacks and never modifies the BirthMachine's configured 45-second timeline: PASS
  - F12 reveals a panel that reports all three states. The panel starts hidden in debug builds and is not created when the engine reports a release build: PASS
  - Real Godot 4.7.1 completed the focused acceptance with no leak warning: PASS
  - With all three flags active, the real UI completed seven build decisions, four operation decisions, four organ observations, three carryovers, stage-four threshold feedback, the birth gate, all five real birth windows, first breath, and the ending route: PASS
  - The full lifecycle run ended with REGRESSION PASS after the actual 45-second birth sequence and no leak warning: PASS
  - A rendered no-art build-decision capture kept stage, candidate, cost, resource, instruction, and control text readable over same-size placeholder art: PASS
  - Godot import retained window/dpi/allow_hidpi, window/stretch/aspect, integer scaling, and every prior rendering setting; project.godot differs only by the declared DebugFlags autoload: PASS
interface_changes: |
  Registers DebugFlags at res://core/debug_flags.gd. Public controls:
    set_animations_disabled(value)
    set_audio_disabled(value)
    set_formal_art_disabled(value)
    toggle_animations()
    toggle_audio()
    toggle_formal_art()
    set_all_disabled(value)
    state_snapshot()
    presentation_window_remaining_sec()

  Debug-build shortcuts are F8 for animation, F9 for audio, F10 for formal art,
  and F12 for the status panel.
reported_not_fixed:
  - Dedicated D-19a build-completion and system-collaboration sprite sheets do not exist. D-23 intentionally defines their fallbacks from landed organ states, routes, resource shapes, highlights, and text.
  - The base runtime has no animated-mode eight-second or twelve-second presentation gate. T-38 enforces those locked durations when animation is disabled, which is the scope of its fallback contract; it does not redesign the animated presentation owned by D-19a.
completed_at: 2026-07-29
