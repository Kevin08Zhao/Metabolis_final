task_id: T-34
task_name: Step-by-step guidance copy
owner: ACCOUNT_C
status: DONE
base_main_commit: eb44a6e
source: docs/prompts/Metabolis_Prompts_Full_v2.md · T-34 · step-by-step guidance copy, following the two-layer structure of T-33
upstream:
  - docs/coord/done/T-33.md (status DONE) - the guidance step definition table
  - docs/coord/done/T-07.md (status DONE) - docs/SCIENCE_NOTES.md
  - docs/coord/done/T-30.md (status DONE) - the G1 capacity, measured
outputs:
  - docs/UI_COPY.md
checks:
  - One line for every entry of the T-33 definition table. Twenty stage lines, eighteen action lines, three skip lines, forty-one in all: PASS
  - The five stage readings are separate lines for all four stages and are never merged: PASS
  - Every line is at most the G1 per-line maximum of 26 characters, counted including spaces and punctuation: PASS
  - Every stated character count matches the actual length of the line beside it: PASS
  - Every line is one sentence, with no semicolon joining two things: PASS
  - No line uses an exclamation mark: PASS
  - No line addresses the player in second person: PASS
  - Every line is classed as naming a structure or pointing at an action; none is classed as explaining a principle: PASS
  - The five terms banned by T-33a appear nowhere, checked term by term against the six decision lines on their own as well as against all forty-one: PASS
  - No line indicates that one candidate or one plan is preferable: PASS
  - The skip set is three lines, within the cap of three: PASS
  - Copy introduces no structure or mechanism outside docs/SCIENCE_NOTES.md: PASS
  - Live run on Godot 4.7.1.stable, headless, 0 script errors, 87 checks, 86 PASS and 1 deliberate self-test FAIL: PASS
where_the_character_limit_comes_from: |
  The prompt leaves the cap as a blank to be filled with the per-line maximum of
  table G1. docs/UI_COPY.md records that as 26 characters, and
  docs/UI_LAYOUT.md section 7 derives it from the engine font at 8 px in the
  smallest of the three information containers.
  Guidance takes the same 26 rather than a looser figure because it shares the
  shortest container in the layout. Unlike G1 it gets one line, not two, since
  the prompt requires one sentence per entry.
  Six lines sit exactly at 26 and are listed in the closest-to-the-limit section
  of docs/UI_COPY.md, alongside the three G1 lines T-31 already put there.
language: |
  The lines are written in English. Repository policy requires it, and
  docs/UI_COPY.md already records the same decision in its scope note: the copy
  it holds is the English equivalent of the player-facing copy the prompt asks
  for. The character counts are of the English lines, measured against the same
  26-character container.
  This is worth stating because a 26-character cap is far tighter in English than
  the same count of full-width characters. Every line was written to fit rather
  than trimmed afterwards, and the longest six are at the limit exactly.
the_four_column_table: |
  Required by the acceptance: entry number, copy, actual character count, and
  whether the line names a structure or points at an action. It is in
  docs/UI_COPY.md under "Guidance Copy", split into three tables so each keeps
  its own keys. The driver reprints all forty-one rows in one numbered table
  after parsing them back out of the shipped file.

  Distribution of the fourth column: 16 lines name a structure, 25 point at an
  action, 0 fall in any third category. The checker treats a third category as a
  failure rather than as a warning, so "explains a principle" cannot pass by
  being unnoticed.
the_end_judgement: |
  The prompt closes by asking whether, with all guidance copy deleted, a player
  can still complete the stage-one build decision from the interface alone.

  Yes. It was answered by running it rather than by argument.

  With every line blanked, and again with the guidance skipped outright, nothing
  is locked: src/ui/tutorial.gd refuses to close the build candidates or the
  allocation entry under any circumstance, and skipping releases every lock it
  held. Both stage-one candidates keep the data a player actually reads:

    cluster_compact  network_efficiency 1.12  build_duration 31.0  future_convenience 0.98
                     cost 28.0 / 26.0 / 16.0
    cluster_wave     network_efficiency 0.96  build_duration 24.0  future_convenience 1.04
                     cost 24.0 / 22.0 / 12.0

  Those are runtime values from Balance rendered by T-13a against table D8, not
  copy, so deleting copy does not touch them. The confirm control and its
  rejection feedback are unchanged.

  What is lost is the naming, not the ability. A player can still pick a card,
  place it and confirm, but nothing says the cluster is a cluster or that the
  stage wants one built. That is the intended split: guidance names and points,
  and the interface carries the mechanism.
how_the_copy_was_checked: |
  The checker parses the lines out of docs/UI_COPY.md itself rather than holding
  a second copy of them. A checker fed its own transcription proves nothing about
  the file that ships, and the stated character counts in the table are compared
  against the measured length of the line beside them, so a count edited without
  its line is caught.

  Each of the six content rules is shown to fire on a planted violation before
  its zero findings over the real copy are believed. The banned-term and
  second-person rules match on word boundaries, and both were confirmed to
  ignore "Betterment", "suggestive" and "young" while catching "better",
  "should choose" and "your". This is the third time this account has needed the
  word-boundary guard; T-25 and T-32 both had a check fire on a substring.
copy_names_structures_that_balance_identifies: |
  The structure names are player-facing names for the structures whose
  identifiers live in organs.required_ids_by_stage. They are not a second copy of
  that list: one holds identifiers, the other holds what a player reads.
  The obligation this creates is recorded in docs/UI_COPY.md - adding a structure
  to a stage needs a line here as well as a value there. It is a real maintenance
  cost and was accepted rather than hidden, because the alternative was a runtime
  list frame repeated identically across all four stages, which would have made
  twelve of the twenty stage lines the same sentence.
three_lines_are_identical_and_should_be: |
  "Three decisions to settle" appears for stages two, three and four. Each of
  those stages carries two build decisions and one operation decision, so it is
  the same sentence rather than three that resemble each other. Stage one differs
  because it carries one build decision, and reads "Two decisions to settle".
  The counts were read from chapters.<stage_id>.required_build_decision_ids and
  required_operation_decision_ids, not from the prose of the timeline document.
the_skip_set: |
  Three lines, at the cap. Each carries something a beginning player cannot start
  without and cannot read off the screen:

    Map cards are the choices    the cards are the decision, not decoration
    Confirm settles a decision   confirmation is irreversible per table D4
    The task can be skipped      nothing on screen says the task is optional

  The third earns its place because a player who believes the task is required
  may stall on it, and table M6 of docs/MINIGAME_SPEC.md guarantees it never
  gates anything. Everything else a beginner needs is already visible: the six
  resource readings, the timeline, the three operational metrics, the candidate
  cards with their values and costs, and the confirm control.
observed:
  - "self test: the deliberately false check reported FAIL and the true one PASS before any verdict was read"
  - "second self test: each of the six content rules fired on a planted violation and stayed silent on a near miss"
  - "41 lines parsed out of docs/UI_COPY.md, no key twice"
  - "five separate readings confirmed for each of stage_origin, stage_harbor, stage_circulation and stage_birth"
  - "eighteen action lines, three for each of the six action guides named by Tutorial.ACTION_GUIDES"
  - "skip set: 3 lines, cap 3"
  - "over limit 0, mismatched counts 0, semicolons 0, exclamation marks 0, banned terms 0, second person 0, multi-sentence 0, third category 0"
  - "six decision lines checked on their own for banned terms: zero hits"
  - "with the copy blanked and again with guidance skipped, locked_target_ids() held neither build_candidate_cards nor resource_allocation_entry"
  - "final tally: pass 86, fail 1, the one failure being the harness self test"
reported_not_fixed: |
  Nothing reads these keys yet. src/ui/tutorial.gd still emits its own
  square-bracket placeholders, and wiring it to docs/UI_COPY.md is integration
  work, exactly as it is for the T-30 and T-30a keys T-31 delivered. The keys
  here are named after the placeholders the tutorial already produces, so the
  wiring is a lookup rather than a redesign.

  T-35 is the science review and will read this copy alongside SCIENCE_NOTES.md
  and UI_COPY.md's earlier sections. Two lines are the ones to look at hardest:
  "Weeks 9-38: the lungs form" compresses a long process into one phrase, and
  "Weeks 2-3: the blastocyst" names the blastocyst for a stage that also covers
  the germ layers. Both are within what SCIENCE_NOTES.md supports, but both are
  compressions and the review should say so or say otherwise.
notes: |
  Only docs/UI_COPY.md was modified, which this account owns through T-31 and
  which the task queue names as this task's output. No file owned by another
  account was touched. src/project.godot is unchanged and its git diff is empty.
  The acceptance driver was deleted before the commit.
  docs/coord/rework/ holds four OPEN files, all reported by ACCOUNT_D against
  D-09, D-13b, D-28 and D-29. None targets an upstream of this task.
completed_at: 2026-07-28T10:40:00-04:00
