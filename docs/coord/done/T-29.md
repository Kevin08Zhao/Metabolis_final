task_id: T-29
task_name: Six-resource status bar
owner: ACCOUNT_C
status: DONE
base_main_commit: 74006f0a1d0014ac64ceb20f1c1792d2b514e2dc
source: docs/prompts/Metabolis_Prompts_Full_v2.md - T-29
upstream:
  - docs/coord/done/T-18.md (status DONE)
  - docs/coord/done/T-05e.md (status DONE)
  - docs/coord/done/D-14.md (status DONE)
  - docs/coord/done/D-15.md (status DONE)
  - docs/UI_LAYOUT.md (resource status bar)
  - docs/OPERATION_SPEC.md (tables E4 and E8)
outputs:
  - src/ui/resource_bar.gd
  - src/ui/resource_bar.gd.uid
declared_changes_outside_the_primary_output: |
  The project owner explicitly authorized correction of the upstream Balance
  omission before T-29 resumed. docs/BALANCE.json gains
  assist.ui.resource_change_highlight_sec = 0.35, and
  docs/BALANCE_VALIDATION.md documents its unit and validation rule.
  docs/coord/done/T-06.md records the repair and revalidation. The corresponding
  T-06 rework record moves from OPEN to RESOLVED. The generated resource-bar UID
  is the Godot companion to the one requested production script.
checks:
  - The acceptance verifier first evaluated deliberate `1 == 2` and reported the
    expected failure before any implementation verdict was trusted: PASS
  - All six resources initialize and remain visible in the locked order:
    nutrient energy, cell material, developmental signal, waste, stability, and
    knowledge badge count: PASS
  - Knowledge badges show only their integer count and no maximum: PASS
  - At the 640-pixel reference width, container layout produces six 96-pixel
    cells at x 12, 116, 220, 324, 428, and 532 without storing those x positions
    in production code: PASS
  - At 960 pixels, the six expanded cells receive 149 or 150 pixels each due to
    integer rounding, differ by at most one pixel, do not overlap, and end at the
    locked 12-pixel right margin: PASS
  - The bar and all full-cell children use Control anchors and containers. The
    script contains no absolute position assignment and no per-frame `_process`
    resource poll: PASS
  - Every D-15 icon variant is a separately named TextureRect and loads from its
    fixed same-name PNG path, so art can be replaced without changing this
    script: PASS
  - All six base icons remain pairwise distinct after conversion to grayscale:
    PASS
  - Stability crossed stable to strained to critical through
    stability_band_changed. Both the icon variant and the text label changed,
    and each value change highlighted: PASS
  - Waste crossed the warning threshold, overflowed, then recovered. Its
    near-cap and overflow labels and D-15 warning shape were visible, and its
    grayscale shape differs from the spendable-resource shortage shape: PASS
  - A nutrient-energy shortage raised and cleared through threshold events. The
    sufficient/insufficient icon, `[low]` label, value, and highlight updated:
    PASS
  - resources_settled updates all supplied totals, and
    build_decision_confirmed subtracts the three supplied spendable costs: PASS
  - Every changed value flashes once for 0.35 seconds read from
    assist.ui.resource_change_highlight_sec; the value is positive and shorter
    than the settlement tick: PASS
  - Real Godot 4.7.1 headless acceptance completed with RESULT PASS. The expected
    missing-audio warnings are unrelated to this no-audio task: PASS
  - Real Godot 4.7.1 OpenGL/Metal rendering saved the state strip in color and
    grayscale. Both were inspected at original resolution; all six readings fit,
    and non-color distinctions remained readable: PASS
  - Temporary acceptance scenes, generated acceptance UID, and visual QA images
    were removed after verification: PASS
  - src/project.godot is unchanged: PASS
post_sync_smoke: |
  Before commit, the branch fast-forwarded to remote main
  74006f0a1d0014ac64ceb20f1c1792d2b514e2dc. That upstream commit added the
  D-25 stable heartbeat WAV and opened
  docs/coord/rework/T-37__from_ACCOUNT_D_d25_heartbeat_timing.open.md.
  The cleaned project still parsed and exited with code 0 and no script error,
  but verbose Godot shutdown reported one AudioStreamWAV and one
  AudioStreamPlaybackWAV reference. This is the exact upstream T-37/D-25 issue
  recorded by that OPEN marker; T-29 neither loads nor owns audio.
scene_setup:
  - Add one Control named ResourceBar under the persistent CanvasLayer.
  - Anchor it TOP_WIDE in the top resource region and attach
    res://ui/resource_bar.gd.
  - Add no positioned children. The script builds the margin, row, six cells,
    named icon variants, labels, and change-highlight panels in `_ready`.
  - Keep Balance and EventBus registered as autoloads; the bar reads initial
    values and highlight timing from Balance and reacts to EventBus signals.
manual_test:
  - Emit stability_band_changed from stable to strained and then strained to
    critical. Expect `[strained]` and `[critical]`, their matching D-15 icons,
    the new numeric value, and one short highlight at each transition.
  - Set waste to the warning threshold through resources_settled, then emit
    waste_overflowed. Expect `[near-cap]`, then `[overflow]`, the waste-specific
    warning silhouette, the value, and a short highlight.
  - Emit resource_shortage_raised and resource_shortage_cleared for one
    spendable resource. Expect `[low]` plus its insufficient icon, then the
    normal value and sufficient icon, with one short highlight per value change.
  - Repeat the state checks in grayscale and at a wider window size. All six
    cells must remain distinct, inside the bar, and visible without scrolling.
notes: |
  No audio asset is part of T-29. Scene assembly instructions also appear at the
  top of resource_bar.gd so the requested production deliverable remains
  self-contained.
reported_not_fixed:
  - The newly arrived T-37/D-25 heartbeat cadence, crossfade, and shutdown
    reference issue remains OPEN and must be repaired as its own task and commit.
    Mixing src/core/audio_router.gd or audio generation into T-29 would violate
    the fixed one-task/one-commit workflow.
completed_at: 2026-07-28T15:03:00-04:00
