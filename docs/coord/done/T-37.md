task_id: T-37
task_name: Event-driven audio playback layer
owner: ACCOUNT_C
status: DONE
base_main_commit: 88041591dbfb688449484e66fa0cc8fc6aeb24a3
source: docs/prompts/Metabolis_Prompts_Full_v2.md - T-37
upstream:
  - docs/coord/done/T-08.md (status DONE; corrected and revalidated here)
  - docs/coord/done/T-36.md (status DONE)
  - docs/EVENT_API.md
  - docs/CONTEXT.md naming conventions
outputs:
  - src/core/audio_router.gd
  - src/core/audio_router.gd.uid
  - docs/AUDIO_NAMING.md
declared_changes_outside_the_primary_outputs: |
  The project owner explicitly authorized repair of the upstream blockers before
  implementation. docs/BALANCE.json and docs/BALANCE_VALIDATION.md gain the
  one-shot concurrency and high-frequency interval required by the prompt;
  docs/coord/done/T-06.md records that revalidation. docs/EVENT_API.md gains the
  knowledge_entry_closed declaration that T-30 had added to its event table and
  EventBus; docs/coord/done/T-08.md records the stale thirty-nine-signal claim.
  src/project.godot registers AudioRouter because T-37 explicitly requires an
  autoload singleton. No audio asset is included.
checks:
  - Acceptance verifier self-test detected the deliberate `1 == 2` failure before
    any T-37 verdict was trusted: PASS
  - Balance structure - BALANCE.json parses, still has exactly twelve top-level
    keys, and all keys remain snake_case: PASS
  - Balance values - assist.audio.max_concurrent_one_shots resolves to the
    positive integer 6; assist.audio.high_frequency_min_interval_sec resolves to
    0.1 seconds, positive and no greater than tick_interval_sec: PASS
  - Event contract - the EVENT_API table, paste-ready declaration block,
    EventBus declarations, and EventBus.EVENT_NAMES contain the same forty names;
    EventBus.verify_signal_set returns no missing or extra names: PASS
  - Derived naming - every one-shot path is computed as
    audio/events/<event_name>.wav; the router contains no event-to-file map and
    AUDIO_NAMING.md enumerates all forty exact results: PASS
  - Empty audio tree - real Godot 4.7.1 emitted all forty events with no audio
    files. Every gameplay signal reached an independent listener, no one-shot
    started, and the run ended with only [AUDIO] missing-file warnings and
    `[T-37 FULL EMPTY ACCEPTANCE] RESULT PASS`: PASS
  - Arbitrary waveform - temporary one-second PCM WAV files placed under derived
    names loaded through AudioStreamWAV, and build_decision_confirmed started a
    one-shot player: PASS
  - High-frequency debounce - two immediate action_rejected emissions started one
    playback; another emission after 0.12 seconds started the next playback,
    proving the 0.1-second Balance interval: PASS
  - Pool ceiling - a burst across multiple event names reached exactly six active
    players and reused the pool without ever exceeding the Balance limit: PASS
  - Ambient bed - a temporary heartbeat_bed.wav looped on the dedicated player;
    stability band 0 to 2 faded its level from -16 dB to -8 dB without replacing
    the stream or applying processing other than a fade: PASS
  - Mute - set_muted(true) stopped the ambient bed and all one-shots. A
    season_completed gameplay listener still ran while muted, and unmuting
    restarted the ambient bed: PASS
  - Cleanup - both acceptance scenes, their generated UIDs, all temporary WAV
    files, and the temporary audio subdirectories were removed: PASS
  - src/project.godot - Godot import preserved every existing display and
    rendering setting; the only diff is the AudioRouter autoload line: PASS
interface_changes: |
  Registers AudioRouter at res://core/audio_router.gd. Public integration points:
    event_audio_path(event_name: StringName) -> String
    toggle_muted() -> bool
    set_muted(value: bool) -> void
    active_one_shot_count() -> int
    one_shot_pool_size() -> int

  One-shots use audio/events/<event_name>.wav. The independent ambient bed uses
  audio/ambient/heartbeat_bed.wav. Missing paths warn once and skip playback.
  HIGH_FREQUENCY_EVENTS is the exact thirteen-event set marked repeatable within
  one tick by EVENT_API; it is not a filename mapping.
reported_not_fixed:
  - Audio production is intentionally absent. D-24 selects at most twelve event
    files from the forty deterministic paths, and D-25 may tune the heartbeat
    bed against final animation timing.
  - T-29's resource-change highlight Balance omission remains represented by
    docs/coord/rework/T-06__from_ACCOUNT_C_t29_highlight.open.md and was not
    changed during T-37.
completed_at: 2026-07-28T14:46:03-04:00
revalidations:
  - revalidated_at: 2026-07-28T15:19:00-04:00
    base_main_commit: 9b852019fcd3e7a8c979ba7901643095d1abaf07
    performed_by: ACCOUNT_C
    trigger: Account D's D-25 three-band heartbeat timing rework
    declared_changes_outside_the_original_output: |
      The project owner authorized correction work. The repair extends Account
      D's deterministic D-25 generator, tests, two state-specific WAV outputs,
      heartbeat specification, sound inventory, manifest, attribution entry,
      and asset checker so the runtime contract and asset contract agree.
      D-25 ownership does not move; its manifest is marked ready for owner
      revalidation rather than DONE.
    checks:
      - The accepted stable WAV retains its exact SHA-256: PASS
      - Stable, strained, and critical loops are exactly 1,000, 440, and 1,800 ms, matching D-21: PASS
      - All loops remain deterministic mono 48 kHz PCM16, peak at 7,600, have low DC offset, and begin and end with silence: PASS
      - Runtime uses three fixed paths and never changes pitch_scale: PASS
      - The target state waits for the current relaxed-frame-0 loop boundary, then two players overlap for the full 250 ms crossfade: PASS
      - Repeated stability samples inside Table E4 hysteresis zones emit no new event and cause no audio transition: PASS
      - Muting stops both ambient players and all one-shots while an independent gameplay listener continues to receive events: PASS
      - Worst-case two-stream critical overlap remains below full-scale and every bed target remains below a 0 dB one-shot: PASS
      - Normal Godot 4.7.1 shutdown after the documented audio-release grace reports no leaked ObjectDB instance: PASS
      - Temporary acceptance files were removed and src/project.godot remained unchanged: PASS
    interface_additions:
      - ambient_audio_path(stability_band) -> String
      - current_ambient_band() -> int
      - pending_ambient_band() -> int
      - ambient_transition_count() -> int
      - ambient_player_count() -> int
      - ambient_crossfade_duration_sec() -> float
      - ambient_loop_duration_sec(stability_band) -> float
      - prepare_for_shutdown() -> void
