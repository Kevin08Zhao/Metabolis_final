task_id: T-15
owner: ACCOUNT_B
status: DONE
base_main_commit: f2c07f5603508b3ab803a9abba7a655a410d4c03
outputs:
- src/world/network_intervention.gd
checks:
- Passes the selected build candidate to NetworkBuilder without editing network topology directly: PASS
- Applies the E1 capacity formula using the configured increment, tier multiplier, pressure response, and capacity clamps: PASS
- Deducts the configured development-signal cost in the same atomic capacity transaction: PASS
- Rejects inactive, immutable, missing, unaffordable, and invalidly configured edge interventions without changing capacity or resources: PASS
- Accepts and preserves an ordered list of multiple active mutable bottleneck edges without rerouting them: PASS
- Limits trunk direction, capacity increase, and bottleneck priority to the configured number of uses per stage: PASS
- Exposes disabled-entry state through is_available after each intervention is exhausted: PASS
- Rejects manual edits to existing routes with visible feedback, action_rejected, and a [NET] console explanation: PASS
- Emits the canonical transport_network_intervened event after a successful capacity change: PASS
- Focused Godot test covers all three interventions, repeated-use rejection, the capacity calculation, resource cost, event emission, and forbidden manual route editing: PASS
interface_changes: |
  Adds class NetworkIntervention at src/world/network_intervention.gd.
  Public interface:
    configure(balance_access: Node, event_bus: Node) -> void
    is_available(stage_id: StringName, intervention_id: StringName) -> bool
    select_trunk_direction(stage_id, organ_id, decision_id, option_id, network_builder) -> Dictionary
    increase_edge_capacity(stage_id, selected_edge, active_transport_edge_ids, mutable_transport_edge_ids, transport_pressure, resources) -> Dictionary
    prioritize_bottleneck_edges(stage_id, ordered_edge_ids, active_transport_edge_ids, mutable_transport_edge_ids) -> PackedStringArray
    reject_manual_route_edit(edge_id: StringName) -> bool
    reset_stage(stage_id: StringName) -> void
completed_at: 2026-07-28T11:40:00+08:00
