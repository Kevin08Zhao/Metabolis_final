task_id: T-19d
owner: ACCOUNT_C
status: DONE
base_main_commit: 1d11c90da364a5d2f30bcb1ea4aa2519d96169a6
source: docs/prompts/Metabolis_Prompts_Full_v2.md · T-19d · chapter flow state machine
upstream:
  - docs/coord/done/T-05a.md through T-05f.md (all six spec-first tasks, status DONE)
  - docs/coord/done/T-09.md (status DONE)
  - docs/coord/done/T-10.md (status DONE)
  - docs/coord/done/T-11.md (status DONE)
  - docs/CHAPTER_TIMELINE.md (full text, pasted)
  - docs/CARRYOVER_SPEC.md (table F1 carryover values and mapping rules, pasted)
outputs:
  - src/core/chapter_flow.gd
  - docs/CHAPTER_FLOW_STEPS.md
checks:
  - Ten steps are an enum and every transition routes through advance_to; _current_step is assigned in exactly one place and no other function writes it: PASS
  - Each step's opening condition is read from configuration through Balance at chapters.<stage_id>.*, not hardcoded in logic. Table C1 of docs/CHAPTER_FLOW_STEPS.md lists all ten: PASS
  - Stage one having a single build decision falls out of chapters.stage_origin.required_build_decision_ids having one entry; no stage name is branched on. Observed - stage one runs build_decision to build_completion to operation_decision with no return to step five, while stages two through four loop steps five and six twice: PASS
  - Stage four performing no step-ten carryover falls out of chapters.stage_birth.next_stage_id being null. Observed - across the full run there are three stage_advanced, three stage_snapshot_written and three carryover_applied, none of them from stage four, which instead reaches run_complete: PASS
  - Step three is skippable and never blocks. docs/CHAPTER_TIMELINE.md states the minigame does not appear in the stage-exit condition and that skipping, completing, or never entering it all leave the main line free to advance, so the step always closes: PASS
  - Every advance prints [FLOW] stage number, old step, new step: PASS
  - The matching EVENT_API events are emitted - phase_changed on every real step change, stage_advanced on leaving step ten with a next stage, stage_loaded after the entered stage is built, and stage_snapshot_written plus carryover_applied from the carryover placeholder: PASS
  - No step's internal logic is implemented. Each step calls a placeholder that returns immediately and records only the outcome the next gate reads; each placeholder names the task that replaces it: PASS
  - No data structure script was modified and no plugin was introduced: PASS
  - Original prompt acceptance item, ten-step opening-condition table - table C1 of docs/CHAPTER_FLOW_STEPS.md, with the stage-exit boolean broken out as table C2 and the two specials as table C3: PASS
  - Original prompt acceptance item and day-three gate check two, running from the stage one opening through to stage two step one without debug shortcuts, without manually changing state variables, and without errors - reached stage two step one after ten advances; continuing the same driver reached run_complete after forty-six advances with no stall and no error: PASS
  - Live run on Godot 4.7.1.stable, headless: PASS
interface_changes: |
  Adds class ChapterFlow (Node) at src/core/chapter_flow.gd.
  Public interface:
    start_new_run() -> bool
    advance() -> bool
    advance_to(next_step: int) -> void        # sole mutator of the current step
    can_exit_current_step() -> bool
    is_stage_exit_ready() -> bool
    current_step() -> int
    current_step_id() -> StringName
    current_stage_id() -> StringName
    stage_number() -> int                     # one-based
    is_run_complete() -> bool
    signal run_completed(final_stage_id: StringName)
    enum Step with ten values; const STEP_IDS maps them to the phase strings
    stored in ChapterData.phase
  Downstream tasks replace the placeholder bodies in place; the step routing and
  gating do not change when they do. Owners are named per placeholder and listed
  in table C1.
blocking_issue: |
  main cannot run as delivered. src/core/chapter_flow.gd requires the EventBus and
  Balance autoloads, and neither registration is committed to src/project.godot.
  T-10 excluded its registration by the project owner's instruction, and
  docs/coord/done/T-11.md records that T-11 likewise left project.godot unchanged
  under the one-production-file constraint. There are now three consumers of two
  unregistered autoloads and no task in any account's queue that owns the file.
  Both entries are required:
    [autoload]

    EventBus="*res://autoload/event_bus.gd"
    Balance="*res://autoload/balance.gd"
  This account holds both lines in its local working tree for acceptance runs but
  has not committed them. Someone must be given ownership of src/project.godot
  before an integration build can start.
notes: |
  One deviation from a first reading of the prompt is recorded here. An initial
  implementation gated step three on the minigame resolution leaving its pending
  value, which deadlocked stage one because the placeholder never resolves it.
  That gate also contradicted docs/CHAPTER_TIMELINE.md, which excludes the minigame
  from the stage-exit condition. Step three now always closes, which additionally
  makes stage four ordinary rather than special - a stage with no minigame behaves
  exactly like a stage whose minigame was skipped.
  advance_to does not emit phase_changed when the step does not actually change.
  Entering the first stage establishes the initial step rather than changing it,
  and docs/EVENT_API.md fires phase_changed only on a real change. That case is
  logged as "(start) -> observe_state" and not emitted.
  No file owned by another account was modified. docs/coord/rework/ contains no
  OPEN file against any upstream of this task.
completed_at: 2026-07-27T17:05:00-04:00
