task_id: T-19c
owner: ACCOUNT_B
status: DONE
base_main_commit: bf191a0a38595c01d62d3ecca0f012d7ceb0a4e0
outputs:
- src/sim/scripted_challenge.gd
checks:
- Selects only the three configured E7 bottleneck introductions and never injects stage_birth: PASS
- Enforces the configured per-stage and total injection limits at one and three or less: PASS
- Requires the configured stage, stable trigger, active systems, unconfirmed required operation, and no blocking modal: PASS
- Immediately skips and permanently records a challenge when its bottleneck was learned naturally before the trigger: PASS
- Rejects scripted-source appearances from natural history and prevents late natural history from changing an injected challenge to skipped: PASS
- Selects every target deterministically with the specification tie-break: PASS
- Reads every intensity and recovery bound from Balance and passes them unchanged to the runtime transaction: PASS
- Commits history only after the caller applies the state and invokes normal E7 detection successfully: PASS
- Prevents callback re-entry from injecting the same challenge or a second challenge in the stage: PASS
- Resolves only after one atomic clear-and-detect transaction proves full E7 recovery and removal of the scripted effect: PASS
- Exposes complete snapshot and restore state, rejects snapshots during external transactions, and prevents replay from applying a challenge twice: PASS
- Uses no disease terminology, randomness, hint dependency, visual implementation, or new event name: PASS
interface_changes: |
  Adds class ScriptedChallenge at src/sim/scripted_challenge.gd.
  Public interface:
    configure(balance_access: Node) -> void
    record_bottleneck_appearance(bottleneck_id: StringName, source: StringName) -> void
    try_inject(stage_id: StringName, context: Dictionary, apply_and_detect: Callable) -> Dictionary
    try_mark_resolved(challenge_id: StringName, clear_and_detect: Callable) -> bool
    snapshot_state() -> Dictionary
    restore_state(state: Dictionary) -> void
  Read-only property:
    challenge_history
completed_at: 2026-07-28T12:25:00+08:00
