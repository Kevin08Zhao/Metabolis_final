task_id: T-30a
task_name: Chapter summary screen
owner: ACCOUNT_C
status: DONE
base_main_commit: f4d36ce
source: docs/prompts/Metabolis_Prompts_Full_v2.md · T-30a · chapter summary screen
upstream:
  - docs/coord/done/D-17.md (status DONE) - table G3
  - docs/coord/done/T-05a.md (status DONE) - docs/CHAPTER_TIMELINE.md
  - docs/UI_LAYOUT.md section 7, table G3 (capacity, transcribed)
outputs:
  - src/ui/chapter_summary.gd
  - src/ui/chapter_summary.gd.uid
declared_changes_outside_the_single_file: NONE
checks:
  - Harness self-test - a deliberate `1 == 2` check is reported FAIL, a `1 == 1`
    check is reported PASS, and the comment stripper removes a trailing
    `# ScrollContainer` while preserving `"# not a comment"` inside a string
    literal. All three held before any verdict below was read: PASS
  - Table G3 transcription - 544 by 304, border 2, padding 12 by 8, header 20
    plus 4 gap, item gap 4, font 8, line height 10, 6 items, 4 lines per item,
    24 total lines, 64 characters per line: PASS
  - Table G3 arithmetic reproduced independently - text width 516,
    floor(516/8) = 64, 6 x 4 x 10 = 240 plus 5 x 4 = 20 gaps equals the 260 of
    available height exactly, and 6 x 4 = 24: PASS
  - Structural item count - the built node tree contains exactly six `Item_*`
    blocks and every block holds exactly four line labels. Checked by walking
    the tree, not by reading the source: PASS
  - No seventh item, no scrolling, no pagination - a recursive class-name walk
    of the built tree finds no ScrollContainer, TabContainer, TabBar, or
    PageIndicator, and a comment-stripped scan of the source finds none of those
    plus no `set_page` or `page_count` in executable code: PASS
  - Overflow truncates and reports - six 100-character lines supplied to one
    item are cut to four lines of 64 characters each, with one `[UI]` line naming
    the item and the line-count overrun and one per over-long line: PASS
  - Omitted item does not borrow - an item the caller leaves out shows the empty
    placeholder rather than taking another item's line allocation: PASS
  - Pause on open - opening sets the scene tree paused (stopping timers), raises
    the pause indicator, and makes `elapsed_for_settlement()` return zero: PASS
  - Reading time is discarded, not banked - two ticks of 1.0 and 2.0 seconds
    while open yield zero elapsed each, `withheld_seconds()` reports 3.0, and
    after close the next tick returns its real 1.0 while `withheld_seconds()`
    stays 3.0. The paused interval is never replayed into settlement: PASS
  - Close resumes - the tree unpauses, the indicator hides, and a second close
    on an already-closed summary is refused: PASS
  - Item 3 is derived, not hardcoded - two different build-decision records for
    the same decision id produce different text, and the emitted line carries the
    chosen option id and slot id read from the record: PASS
  - Item 4 shape - label plus exactly three knowledge points fills the four-line
    allocation; a short set is padded with a missing-point placeholder rather
    than collapsing the item: PASS
  - Stage two dual content inside six items - a stage two summary carrying
    blastocyst positioning, the placental foundation, all three germ layers, both
    build decisions, and three knowledge points opens with exactly six items and
    is compared line by line against what was supplied. Nothing was truncated:
    PASS
  - Stage two sharing is declared in code - `STAGE_TWO_SHARED_ITEM_IDS` names
    exactly the structures item and the system-connection item, and the
    acceptance asserts those two constants rather than trusting the prose: PASS
  - Chapter review re-entry - an unvisited stage cannot be reviewed and its
    reopen is refused; a closed stage can be reviewed, shows the archived
    content, and pauses the game again: PASS
  - src/project.godot - `git diff src/project.godot` is empty after the Godot
    import and the acceptance run; `window/dpi/allow_hidpi` and
    `window/stretch/aspect` are both still present. Not staged: PASS
acceptance_table_six_item_sources:
  note: |
    The six slots are categories fixed by table G3 and by the T-30a prompt body.
    They are the same six in every stage; only the content poured in changes.
  stage_origin:
    current_stage: stage_origin, post-fertilization week 1
    structures_formed: zygote, blastomeres, morula, blastocyst precursor
    system_connection: build_cell_cluster record
    knowledge_points: Section Eighteen item 1
    city_change: construction-zone visual, one cell into a compact cluster
    encyclopedia: entries unlocked in the stage
  stage_harbor:
    current_stage: stage_harbor, post-fertilization weeks 2 to 3
    structures_formed: blastocyst positioning, placental foundation, ectoderm, mesoderm, endoderm
    system_connection: build_placenta_port and build_germ_layer_districts records
    knowledge_points: Section Eighteen items 2 and 3
    city_change: construction-zone visual across both subphases at one map node
    encyclopedia: entries unlocked in the stage
  stage_circulation:
    current_stage: stage_circulation, post-fertilization weeks 4 to 8
    structures_formed: heart, early vessels, neural tube, brain and spinal-cord foundation, background organs
    system_connection: build_heart_pump and build_neural_network records
    knowledge_points: Section Eighteen items 4, 5, and 6
    city_change: construction-zone visual, selectable slots plus background animations
    encyclopedia: entries unlocked in the stage
  stage_birth:
    current_stage: stage_birth, post-fertilization weeks 9 to 38
    structures_formed: pulmonary gas-exchange region, pulmonary-circulation interface
    system_connection: build_lung_exchange and build_pulmonary_interface records
    knowledge_points: Section Eighteen items 7, 8, and 9
    city_change: construction-zone visual, whole-body check into birth and first breath
    encyclopedia: entries unlocked in the stage
stage_two_dual_content_answer: |
  docs/CHAPTER_TIMELINE.md fixes six content facts for stage two: blastocyst
  positioning, the placental foundation, the transport-priority settlement, and
  the three germ layers. Those six facts are not six slots. They are distributed
  across the same six categories every other stage uses, which is why no seventh
  item is needed.

  Shared by both content groups - these two carry the doubling:
    - g3_2_structures_formed: the placental foundation and all three germ layers
      sit in one item together.
    - g3_3_system_connection: both build_placenta_port and
      build_germ_layer_districts records produce a line in the same item.

  Shared in a weaker sense:
    - g3_5_city_change: the construction-zone visual runs across both subphases
      at one map node, so the before/after pair spans them.

  Carrying one group each, or group-independent:
    - g3_1_current_stage names the single stage node stage_harbor for both
      phases together.
    - g3_4_knowledge_points spends its three points across Section Eighteen
      items 2 and 3.
    - g3_6_encyclopedia lists whatever unlocked, from either phase.

  Everything past those six facts - chorionic villi, amnion, yolk sac, primitive
  streak, later germ-layer differentiation - is construction-archive material per
  CHAPTER_TIMELINE.md and does not enter this screen.
reported_not_fixed:
  - The system-connection line embeds three identifiers and its scaffolding is
    charged against the same 64-character budget as copy. The first draft used
    `[connection:%s->%s@%s]`, which pushed the stage two germ-layer line to 67
    characters and truncated it. The wrapper was shortened to `[conn:%s>%s@%s]`,
    9 characters, leaving 55 for the three identifiers. That is enough today -
    the longest locked decision id, `build_germ_layer_districts`, is 26
    characters, leaving 29 for option id plus slot id combined. It is NOT
    guaranteed in general - option and slot ids come from Balance and are not
    length-capped by any spec I could find. A long option/slot pair will still
    overflow and truncate. The decision id is placed first so it survives any
    cut, and the `[UI]` line names the item. Whoever fixes Balance option ids, or
    T-31 when it replaces this placeholder, should keep the combined budget in
    view. Not fixed here because capping identifier length is not this task's
    file.
  - Two documents each say stage two has "six" of something, and they are not the
    same six. docs/UI_LAYOUT.md table G3 fixes six visual category slots.
    docs/CHAPTER_TIMELINE.md line 88 fixes six content facts for the stage two
    summary. Reading them as the same list would be a contradiction. The T-30a
    prompt body settles it by enumerating the six as the categories, and its own
    acceptance question - which items do the two groups share - only makes sense
    under that reading. Recorded here so the next account does not re-open it.
    No document was edited.
  - Not verified by this task - whether the six items are legible at the locked
    8-pixel font with real Chinese copy. Every string in this file is a bracketed
    ASCII placeholder, so the character counts exercised here are ASCII counts.
    D-17 recorded a full-width CJK reference render against table G3 and reported
    PASS; this task did not repeat that measurement and does not claim it.
environment_notes: |
  The host carried eleven orphaned headless Godot processes from earlier
  acceptance runs, the oldest 19 hours old, running _t19a_smoke.tscn,
  _t19d_smoke.tscn, and _audit.tscn. They did not interfere with this run, which
  exited on its own. They are recorded because a driver that never exits may have
  produced a partial log, which would matter for any verdict read from those runs.
  Not acted on; left for the user to rule on.
completed_at: 2026-07-28T11:54:54-04:00
