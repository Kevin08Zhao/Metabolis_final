task_id: T-05b
owner: ACCOUNT_C
status: DONE
base_main_commit: a749898fe2eee53b28d88f68422eedba023ea1fe
source: docs/prompts/Metabolis_Prompts_Full_v2.md · T-05b · MINIGAME_SPEC.md minigame framework specification
upstream:
  - docs/coord/done/T-05a.md (status DONE)
  - docs/coord/done/T-05c.md (status DONE, required by the task guide to force spec ordering)
  - docs/coord/done/T-05d.md (status DONE)
  - docs/coord/done/T-05e.md (status DONE)
  - docs/CHAPTER_TIMELINE.md (full text, pasted)
  - docs/BUILD_DECISION_SPEC.md (cost portion: tables D2 and D4, pasted)
outputs:
  - docs/MINIGAME_SPEC.md
checks:
  - Per-run duration limit is sixty seconds for all three prototypes, stated as a hard upper bound derived from the operating-time budget and explicitly not a two-to-five-minute range: PASS (table M1)
  - Exactly two task levels, tutorial and standard. A challenge level and a comprehensive level are forbidden, including as reserved future keys. The runtime difficulty_tier used by the repeated-failure rule is defined as a runtime state, not a third level: PASS (tables M1 and M2)
  - Rating uses exactly three criteria — completion accuracy, completion efficiency, hint usage count. Resource loss and body stability are explicitly excluded and pointed back at tables E3 and E4 of docs/OPERATION_SPEC.md; a minigame run may not read or write stability, waste, transport pressure, or signal coverage: PASS (table M3)
  - Each of the three prototypes states all five required items — biological process represented, player operation, goal determination, difficulty setting completable within sixty seconds, relationship to the build decision of its own stage: PASS (table M2, five rows by three columns)
  - Reward magnitude is bounded below three tenths of the stage build cost and is written entirely as balance.minigames.<path> placeholders. The bound compares against the cheapest candidate path per decision, so it holds under every candidate choice. Knowledge badges are excluded from the comparison because they cannot be spent on building: PASS (table M4)
  - All six failure rules retained — no regression, organ never removed, no death ending, reason stated, immediate retry, difficulty eased after repeated failures — with scope explicitly limited to the inside of the minigame: PASS (table M5)
  - No minigame is written as a prerequisite for any organ build, any stage advance, or any operation decision, and the enforcement point for each guarantee is named: PASS (table M6)
  - No bare numeric values: every tunable is a balance.minigames.* path. The only stated constants are the locked sixty-second limit and the three-tenths ceiling, both of which the original prompt fixes as hard constraints on T-06: PASS
  - Original prompt acceptance item, executable zero-reward calculation: PASS. The document supplies runnable pseudocode that zeroes all three rewards and walks the four stages comparing available resources against the cheapest required build cost, reusing balance.validation.zero_reward and the baseline birth check already assigned to T-06 by table E5 of docs/OPERATION_SPEC.md rather than introducing a second validation path
  - Original prompt acceptance item, remedy direction: PASS. On failure the document requires raising the failing stage's per_tick_output or tick_count and explicitly forbids raising any minigame reward, with the reason stated
interface_changes: |
  Adds docs/MINIGAME_SPEC.md with numbered output tables M1 through M7. Downstream
  tasks must reference table numbers rather than pasting the document.
  T-19a reads M2 for the runtime contract, M3 for rating, M5 for failure handling,
  and M6 for the skip contract.
  T-29b reads M1 and M6 for task panel states.
  T-06 reads M7 for configuration keys and must satisfy the M4 inequality and the
  closing acceptance calculation.
  This specification also fixes the keys of the rating_detail parameter of
  minigame_rated in docs/EVENT_API.md: exactly completion_accuracy,
  completion_efficiency, and hint_usage_count.
revalidations:
  - revalidated_at: 2026-07-27T17:05:00-04:00
    base_main_commit: 1d11c90
    trigger: T-06 delivered docs/BALANCE.json and docs/BALANCE_VALIDATION.md, so the assertions this task could only state in shape are now measurable, and the known limitation recorded below can be closed.
    measured:
      - M4 reward ceiling, maximum reward against three tenths of the cheapest build cost per stage - stage_origin 9.00 against a 17.40 ceiling on a 58.00 cost floor, stage_harbor 9.00 against 59.40 on 198.00, stage_circulation 8.00 against 61.80 on 206.00 - PASS on all three, tightest margin is stage_origin at 52 percent of its allowance
      - M2 sixty-second completion headroom, target time at base tier against duration_limit_sec times completion_headroom of 48.0 s - cell_division 40.0 s, material_transport 36.0 s, signal_transfer 40.0 s - PASS on all three
      - M1 duration limit of sixty seconds present on all three prototypes - PASS
      - M1 task levels, tutorial for stage one and standard for stages two and three, with no third level configured - PASS
      - M7 key coverage, every key listed in M7 is present in BALANCE.json under the names this specification gave them - PASS
    findings:
      - BALANCE.json contains minigames.runtime.initial_resolution, which table M7 did not list. It is legitimate under T-06 because its source is the MinigameResolution.PENDING state of docs/GAME_RULES.md rather than this specification, but its absence from M7 meant M7 was not a complete view of the minigames subtree, which would have cost T-19a a lookup failure.
      - docs/GAME_RULES.md addresses the same values through a flat singular prefix, balance.minigame.*, while table M7 uses the nested plural form. T-06 bridged the two with the alias table in docs/BALANCE_VALIDATION.md and T-11 implements the resolution. This was not visible from M7 alone.
    changes:
      - Table M7 gains a row for balance.minigames.runtime.initial_resolution, attributed to docs/GAME_RULES.md as its source.
      - Table M7 gains a "Legacy path aliases" subsection recording the three GAME_RULES flat paths and their resolution targets, and stating that the nested form is canonical for new work.
      - The acceptance section replaces its "cannot be evaluated numerically" paragraph with the two measurement tables above.
    scope: |
      Purely additive documentation. No key was removed or renamed, no formula
      changed, no inequality relaxed, and no table renumbered. The contract that
      T-06 was implemented against is unchanged, so docs/coord/done/T-06.md remains
      valid and Account B has nothing to adjust. T-19a and T-29b, both owned by this
      account, gain the missing key and the alias note.
notes: |
  The known limitation recorded when this task first passed - that the zero-reward
  calculation could not be evaluated because docs/BALANCE.json did not exist - is
  closed by the revalidation above.
  No file owned by another account was modified. docs/coord/rework/ contains no
  OPEN file against any upstream of this task.
  The marker path follows the MARKER PROTOCOL on page 5 of the Account C task guide
  PDF (version 2.0).
completed_at: 2026-07-27T16:05:00-04:00
