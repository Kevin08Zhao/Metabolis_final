task_id: T-19f
owner: ACCOUNT_B
status: DONE
base_main_commit: 9d1538c0a98c38e29d43c894b361d232397d1582
outputs:
  - src/sim/operation_decision.gd
checks:
  - Reads the exact four E6 option IDs and per-stage option count from Balance: PASS
  - Reads each allocation, cost, outcome, required total, confirmation limit, tick interval, resource range, and validation tolerance through Balance: PASS
  - Emits resource_priority_changed after a valid selection with the exact EVENT_API parameters: PASS
  - Requires two confirmation presses and leaves resources unchanged after the first press: PASS
  - Locks the decision before external confirmation listeners run and rejects every repeated submission without a second charge or settlement: PASS
  - Enforces the Balance-defined maximum of one confirmed operation decision per stage: PASS
  - Applies the selected transport, waste-processing, and signal shares without adding input: PASS
  - Deducts the selected option cost and applies its resource outcome before one immediate T-17 E3 settlement: PASS
  - Runs one T-18 E4 threshold check after settlement and exposes its emitted events in the operation outcome: PASS
  - Keeps the T-17 resource state and the public ResourcePool synchronized after the transaction: PASS
  - Emits operation_decision_confirmed and operation_result_settled with the exact EVENT_API names and signatures: PASS
  - Publishes deep-copy decision records containing the stage number, stage ID, decision ID, selected priority, allocation, all six submission-time resources, spent resources, settled resources, outcome, and threshold events: PASS
  - Produces six pairwise nine-column non-dominance rows for the four E6 options: PASS
  - Confirms that every E6 pair is non-dominating and that every equal-weight score difference is within 0.15 times the larger score: PASS
  - Focused stub transaction and real T-17/T-18 integration tests: PASS
  - Godot 4.7.1 editor parse and headless project smoke tests: PASS
  - English-only, secret-pattern, and diff checks: PASS
interface_changes: |
  Adds class OperationDecision at src/sim/operation_decision.gd.
  Public interface:
    configure(balance_access, event_bus, chapter, resources, resource_tick, threshold_watcher) -> void
    present_decision(stage_number, decision_id) -> Array[StringName]
    select_priority(operation_id) -> bool
    request_confirmation(settlement_input) -> bool
    confirmation_enabled() -> bool
    feedback_text() -> String
    restore_decision_records(records) -> void
    validate_non_dominance() -> Dictionary
  Read-only property:
    decision_records
completed_at: 2026-07-28T10:05:00+08:00
