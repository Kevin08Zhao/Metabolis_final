task_id: T-19g
owner: ACCOUNT_B
status: DONE
base_main_commit: 3244fa74f43bca1b0b3a4574e398bfb42f5729ba
outputs:
- src/sim/bottleneck_detector.gd
checks:
- Detects transport pressure, waste accumulation, and low signal coverage independently from E7 Balance thresholds: PASS
- Locates every result to a concrete organ ID, edge ID, or both: PASS
- Applies every E7 recovery conjunction and rearms episode hints only after recovery: PASS
- Emits the six canonical bottleneck appearance and clearing events without implementing visuals: PASS
- Triggers the E11 transport and waste hints once per active episode: PASS
- Triggers hint_signal_coordination at most once per stage outside the compensation case: PASS
- Triggers hint_neural_tube_compensation on the first stage_circulation signal gap when transport limits signal in the same settlement, at most once per save: PASS
- Exposes snapshot and restore state for once-per-save and active-episode deduplication: PASS
- Keeps active bottlenecks independent so all three may coexist in one tick: PASS
interface_changes: |
  Adds class BottleneckDetector at src/sim/bottleneck_detector.gd.
  Public interface:
    configure(balance_access: Node, event_bus: Node) -> void
    evaluate(stage_id: StringName, metrics: Dictionary) -> Array[Dictionary]
    snapshot_state() -> Dictionary
    restore_state(state: Dictionary) -> void
  Read-only property:
    active_results
completed_at: 2026-07-28T12:10:00+08:00
