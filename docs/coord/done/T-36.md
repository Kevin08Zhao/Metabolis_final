task_id: T-36
task_name: Asset integration layer
owner: ACCOUNT_C
status: DONE
base_main_commit: 856b918f047ed987ddcfd640615f9a78ac259d05
source: docs/prompts/Metabolis_Prompts_Full_v2.md - T-36
upstream:
  - docs/coord/done/D-04.md (status DONE)
  - docs/coord/done/D-18.md (status DONE)
  - docs/ASSET_SPEC.md
  - docs/ANIM_META_SPEC.md
  - docs/CONTEXT.md naming conventions
outputs:
  - src/core/asset_loader.gd
  - src/core/asset_loader.gd.uid
declared_changes_outside_the_single_file: |
  src/core/asset_loader.gd.uid is the Godot-generated UID companion for the
  production script. No second production file and no project.godot change are
  included.
checks:
  - Acceptance verifier self-test - the deliberate `1 == 2` check reported
    FAIL-EXPECTED, the `1 == 1` check reported PASS, and the comment stripper
    removed a trailing comment while preserving `"# not a comment"` inside a
    string. These checks passed before any T-36 verdict was trusted: PASS
  - Public surface - a comment-stripped structural scan found exactly two
    non-underscore methods, get_static_texture and get_animation_frames: PASS
  - Template-derived static loading - a real production 16 by 16 resource icon
    and an acceptance fixture both loaded by logical name without a hand-written
    logical-name map: PASS
  - Static fallback - a missing logical name returned one 16 by 16 solid-color
    placeholder and printed an [ASSET] warning naming the missing PNG pattern:
    PASS
  - D-18 animation construction - a same-stem 32 by 16 PNG and JSON pair built
    exactly two 16 by 16 AtlasTexture frames, applied loop=true, and preserved
    unequal 125 ms and 375 ms frame durations: PASS
  - Forward-compatible metadata read - the acceptance JSON included an unknown
    future_field; the loader ignored it while using the D-18 frame fields: PASS
  - Partial animation fallback - a PNG with no same-stem JSON returned one
    non-looping placeholder frame and printed `[ASSET] Missing file:
    res://../anim/t36_sheet_missing.json`: PASS
  - Missing animation fallback - an absent PNG returned one non-looping
    placeholder frame and printed `[ASSET] Missing file:
    res://../anim/t36_sheet_absent.png`: PASS
  - Startup batch check - the real manifest scan found 64 expected production
    PNG/JSON files and zero missing. An acceptance-only manifest added one
    deliberately absent item; the scan then reported 65 expected, 1 missing,
    returned that exact path as evidence, and printed its [ASSET] warning: PASS
  - Rename resilience - an integrated acceptance PNG was renamed away, the same
    logical request returned a placeholder without a crash, the warning named
    the missing pattern, and the PNG was restored: PASS
  - Separation of responsibilities - a comment-stripped structural scan found
    no _draw callback, animation play call, or event connection: PASS
  - D-18 validator - tools/check_anim.py reported PASS for the current empty
    production anim directory and for all four contract self-tests: PASS
  - Live runtime - Godot 4.7.1 stable imported the project, ran the complete
    focused headless acceptance scene to `[T36][VERDICT] PASS`, and then started
    the existing main scene for ten iterations with no error: PASS
  - Cleanup - acceptance GDScript, scene, UID, PNGs, JSON, backup, and manifest
    were deleted after the successful run: PASS
  - src/project.godot - the diff is empty after import and both
    window/dpi/allow_hidpi and window/stretch/aspect remain present: PASS
interface_changes: |
  Adds the AssetLoader autoload script contract:
    get_static_texture(logical_name: StringName) -> Texture2D
    get_animation_frames(logical_name: StringName) -> SpriteFrames

  Static logical names follow {category}_{subject}_{variant}. The loader derives
  the filename, recursively searches production directories under art, and
  excludes candidates, reference, and source inputs. Animation logical names
  follow {subject}_{action}_{state}; D-18 resolves each stem to anim/<stem>.png
  and anim/<stem>.json and constructs SpriteFrames at runtime.

  Registration steps are documented in asset_loader.gd. Equivalent
  src/project.godot entry, intentionally not part of this single-file task:
    AssetLoader="*res://core/asset_loader.gd"
specification_resolution: |
  The project owner explicitly confirmed that D-18 controls animation loading:
  AssetLoader uses same-stem PNG and JSON files and does not read the older
  CONTEXT.md .tres animation extension.
reported_not_fixed:
  - AssetLoader is not registered in src/project.godot because T-36 declares one
    production file. This follows the T-10 and T-11 precedent; the source header
    provides the exact Autoload registration steps.
  - The Godot project root is src while art, anim, and docs are repository
    siblings. Local headless Godot can read them through globalized paths, as the
    acceptance proved. Export packaging and inclusion of those external
    directories belongs to T-39 and T-40 and is not changed here.
completed_at: 2026-07-28T14:10:17-04:00
