# Metabolis 项目工作指令

## 适用范围与仓库位置

- 本指令适用于整个项目。
- GitHub 远端仓库（唯一上游来源）：`https://github.com/Kevin08Zhao/Metabolis_final.git`
- 本地工作目录：`/Users/michaelmas/Desktop/Metabolis`
- 所有文件修改、测试、提交操作都必须在上述本地目录中完成；不要直接在 GitHub 网页端修改文件。

## 每次开始工作前：必须先同步远端

在阅读、分析或修改项目文件之前，必须依次完成以下步骤：

1. 进入 `/Users/michaelmas/Desktop/Metabolis`。
2. 确认该目录是目标 Git 仓库，并检查 `origin`：
   - `origin` 必须指向 `https://github.com/Kevin08Zhao/Metabolis_final.git`。
   - 如果本地项目尚不存在，应将该远端仓库克隆到这个目录。
   - 如果目录中已有其他仓库或不明文件，不得覆盖；应停止并向用户说明。
3. 运行 `git status --short --branch`，先识别本地未提交修改。
   - 不得删除、覆盖或擅自暂存用户已有的修改。
   - 不得使用 `git reset --hard`、`git clean -fd` 或其他会丢失工作的命令。
4. 运行 `git fetch origin --prune`，获取远端的最新分支和提交信息。
5. 在当前工作分支有对应远端上游时，运行 `git pull --ff-only`。
   - 必须确认本地已同步到远端最新版本后，才能开始修改。
   - 如果本地与远端已经分叉、存在合并冲突，或未提交修改阻止同步，不得强行覆盖；应保留现场并向用户说明问题。
6. 如果仓库使用 Git submodule，还要运行 `git submodule update --init --recursive`。

## 工作过程

- 默认继续使用用户当前所在分支；除非用户另有要求，不要擅自切换、删除或重命名分支。
- 只修改当前任务所需要的文件，并保留所有无关的现有修改。
- 不得提交密码、令牌、私钥、个人数据、`.env` 内容或其他敏感信息。
- 完成修改后，应运行与改动相关的测试、构建或检查，并如实记录无法运行的验证。
- 提交前必须检查 `git diff` 和 `git status --short`，确认没有把无关文件或生成产物带入提交。

## 每次工作结束：必须提交并上传到 GitHub

只要本次工作产生了文件修改，就必须完成以下收尾流程：

1. 将本次任务相关的文件加入暂存区；不要使用会无差别包含无关文件的暂存方式。
2. 创建内容清晰、范围明确的 Git commit。
3. 再次运行 `git fetch origin --prune`，检查工作期间远端是否出现新提交。
4. 如果远端分支有新提交，先安全地整合远端更新：
   - 优先把本地提交 rebase 到对应的最新远端分支上。
   - 如发生冲突，必须逐项解决、重新验证；不得通过覆盖远端或丢弃本地修改来绕过冲突。
5. 将当前分支推送到 `origin`：
   - 已有关联上游时使用 `git push origin HEAD`。
   - 新分支首次推送时使用 `git push -u origin HEAD`。
6. 推送后确认 GitHub 已包含本次 commit，并检查本地分支与远端是否同步。

禁止使用普通或带租约的强制推送（`--force`、`--force-with-lease`），除非用户针对明确分支和明确原因给予许可。

如果没有产生文件修改，不要创建空提交。如果因为认证、分支保护、冲突、网络或测试失败而无法安全推送，应保留本地 commit，准确说明阻塞原因和当前状态，不能声称任务已经完整上传。
